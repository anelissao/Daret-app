# App
PORT=4000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/daret_api

# JWT
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=1d
JWT_ISSUER=daret-api

# KYC Encryption
# 32-byte key. Prefer hex-encoded 64 chars. Example generated by: openssl rand -hex 32
KYC_ENCRYPTION_KEY=replace_with_64_hex_chars
